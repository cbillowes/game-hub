import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input
      borderRadius={10}
      placeholder="Eg. Grand Theft Auto"
      variant="filled"
    />
  );
};

export default SearchInput;
